<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swhackathon.polystar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".StarQuestionActivity2" />
        <activity android:name=".StarQuestionActivity3" />
        <activity android:name=".StarQuestionActivity4" />
        <activity android:name=".MainActivity" />
        <activity android:name=".InputActivity" />
        <activity android:name=".CalanderActivity" />
        <activity android:name=".StarActivity" />
        <activity android:name=".BookmarkActivity" />
    </application>

</manifest>